DB_TYPE=sqlite
DB_DATABASE=claimflow.sqlite
JWT_SECRET=super-secret-key-change-in-production
PORT=3000

# PostgreSQL deployment settings.
# Set DB_TYPE=postgres and provide these values when using PostgreSQL.
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=password
